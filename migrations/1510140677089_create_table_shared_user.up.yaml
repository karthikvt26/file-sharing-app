- args:
    sql: CREATE TABLE "shared_user"(user_file_id integer NOT NULL, share_user_id integer
      NOT NULL, PRIMARY KEY (user_file_id, share_user_id) )
  type: run_sql
- args:
    name: shared_user
  type: add_existing_table_or_view
